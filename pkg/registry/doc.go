// Package registry provides transport-layer primitives for downloading,
// verifying, and extracting artifacts from GitHub Releases. It is consumed
// by both the plugin system and the workflow pack system.
package registry
