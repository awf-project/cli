name: mcp-proxy-unknown-key-test
version: "1.0.0"
author: test
states:
  initial: start
  start:
    type: agent
    provider: claude
    prompt: "Test prompt"
    on_success: end
    mcp_proxy:
      enable: true
      policy: bogus
  end:
    type: terminal
    status: success
    message: "Done"
