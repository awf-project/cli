name: mcp-proxy-unknown-plugin-test
version: "1.0.0"
author: test
states:
  initial: start
  start:
    type: agent
    provider: claude
    prompt: "Test prompt"
    on_success: end
    mcp_proxy:
      enable: true
      plugin_tools:
        - plugin: nonexistent_plugin
          expose:
            - some_operation
  end:
    type: terminal
    status: success
    message: "Done"
