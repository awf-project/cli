name: mcp-proxy-valid-enabled
version: "1.0.0"
author: test
states:
  initial: start
  start:
    type: agent
    provider: claude
    prompt: "Test prompt"
    on_success: end
    mcp_proxy:
      enable: true
  end:
    type: terminal
    status: success
    message: "Done"
